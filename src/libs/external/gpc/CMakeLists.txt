ADD_LIBRARY(${GPC_TARGET} SHARED gpc.h gpc.c)

install(TARGETS ${GPC_TARGET} 
	RUNTIME DESTINATION ${LIB_INSTALL_DIR}
	LIBRARY DESTINATION ${LIB_INSTALL_DIR}
)
SET_TARGET_PROPERTIES(${GPC_TARGET} PROPERTIES VERSION 2.32)
