ADD_LIBRARY(${TETGEN_TARGET} SHARED tetgen.cxx predicates.cxx)
target_compile_definitions(${TETGEN_TARGET} PRIVATE TETLIBRARY)

install(TARGETS ${TETGEN_TARGET} 
	RUNTIME DESTINATION ${LIB_INSTALL_DIR}
	LIBRARY DESTINATION ${LIB_INSTALL_DIR}
)
